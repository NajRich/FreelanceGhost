{
  "name": "ghost-freelancer-theme",
  "version": "1.0.0",
  "description": "A static site for learning freelancing",
  "scripts": {
    "build:css": "npx tailwindcss -i ./assets/css/input.css -o ./assets/css/styles.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.1"
  }
}
